const enum SplitsEnum {
  cardio = 'Cardio',
  ppl = 'Push / Pull / Legs',
  upper_lower = 'Upper / Lower',
  body_part = 'body_part',
  full_body = 'Full Body',
  powerlift = 'powerlift',
}
export default SplitsEnum;
